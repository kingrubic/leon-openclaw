# 2026-03-26

- Anh Bảo chào mừng Leon đến với Bin Corporation.
- Leon được xác lập là một nhân viên AI chính thức tại Bin Corporation.
- Tên của người dùng: Bảo Nguyễn; cách xưng hô: Anh Bảo.
- Phong cách Leon: bộc trực, thẳng thắn, lễ phép, tận tâm với công việc; chuyên nghiệp, ngắn gọn, đi vào vấn đề.
- Avatar chính thức của Leon đã được người dùng cung cấp và lưu tại `avatars/leon-avatar.jpg`.
- Gương mặt đại diện: nam trẻ, phong thái corporate chuyên nghiệp, tóc đen chải gọn, mặc suit tối màu và áo sơ mi trắng, nền tối trung tính.
- Anh Bảo cung cấp thông tin tài khoản Google cho dự án `kenyaimmigration-org` và yêu cầu Leon lưu lại để dùng về sau. Đã nhận các trường: client_id, project_id, auth_uri, token_uri, auth_provider_x509_cert_url, client_secret.
- Leon đã cấu hình thành công OAuth local callback cho bộ Google credentials của dự án `kenyaimmigration-org`, lấy được token hoạt động và xác nhận có quyền với Google Sheets, Google Drive, Google Docs, và Gmail full scope (`https://mail.google.com/`). Token lưu tại `google-credentials/kenyaimmigration-org.tokens.json`.
- Leon đã xác thực đọc được Google Sheet mẫu, và đã test quyền ghi Drive bằng cách tạo folder `Cào web visa` rồi chuyển file `Tổng hợp tin tức visa` vào folder đó.
- Leon đã dựng runtime riêng ổn định cho `crawl4ai` tại `/Users/admin/.openclaw/tools/crawl4ai-venv` bằng Homebrew Python 3.11, cài Playwright browser runtime, và xác nhận `crawl4ai-doctor` chạy pass.
- Leon đã dựng runtime riêng cho `Scrapling` tại `/Users/admin/.openclaw/tools/scrapling-venv` do `Scrapling` và `crawl4ai` xung đột phiên bản `lxml`; hai tool cần tách môi trường để chạy ổn định lâu dài.
- Leon đã tạo bộ script hỗ trợ Google/OAuth và xử lý sheet/report trong workspace, gồm callback OAuth, đọc sheet, cập nhật báo cáo India e-Visa, và sửa formatting sheet/doc.
- Leon đã hoàn thành báo cáo India e-Visa dựa trên nguồn official `https://indianvisaonline.gov.in/evisa/tvoa.html`, tạo Google Docs báo cáo và tạo/cập nhật tab sheet `25Mar26` trong file giá do Anh Bảo cung cấp; tab này giữ nguyên số không đổi theo `Base` và bôi đậm các ô giá thay đổi.
