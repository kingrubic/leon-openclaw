# 2026-03-27

- Anh Bảo chốt rule mới cho toàn bộ skill cào web visa: report mặc định gồm 6 phần business + 1 mục `TECHNICAL CHANGELOG`; bỏ `EXECUTIVE SUMMARY`; bỏ `TÓM TẮT THAY ĐỔI SO VỚI BASE / ĐẦU VÀO NỘI BỘ` trừ khi được yêu cầu riêng.
- Nếu không có thay đổi thực chất về giá visa, điều khoản, hồ sơ, hiệu lực, ngoại lệ quốc tịch... thì mục `TÓM TẮT THAY ĐỔI SO VỚI HÔM QUA` phải ghi đúng câu: `Không phát hiện sự thay đổi so với báo cáo hôm qua`.
- Các thay đổi kỹ thuật như `0 -> 00` phải đưa sang mục `TECHNICAL CHANGELOG`, không đưa vào phần tóm tắt thay đổi business.
- Anh Bảo chốt scope Australia visa daily-report giai đoạn đầu: `ETA 601`, `eVisitor 651`, `Visitor 600`, `Transit 771`. Nếu nghiệp vụ cần bổ sung phạm vi sau thì sẽ báo thêm.
- Đã chạy bản trial Australia theo scope trên và tạo baseline đầu tiên bằng Google Docs + Google Sheet. Bản thử dùng format report mới: so sánh hôm qua / 7 ngày / từ đầu tháng / visa hiện hành / ghi chú quan trọng / nguồn / technical changelog.
- Anh Bảo chốt thêm rule format cho visa report: các mục lớn `1. ...` đến `7. ...` phải là heading thật; các tên visa/stream như `ETA 601`, `eVisitor 651` chỉ cần bold, không dùng heading.
- Bản trial Australia đã được chỉnh lại Google Docs theo rule trên để làm mẫu trình bày cho các report visa daily tiếp theo.
- Anh Bảo chốt thêm cho report Australia: bổ sung một mục riêng trong Google Docs tên `TIN TỨC VISA TỪ BÁO CHÍ`, rà 2 nguồn `australia.com` và `executivetraveller.com` trong 7 ngày gần nhất; nếu không có tin mới liên quan visa thì ghi đúng câu: `Không phát hiện tin tức mới liên quan đến Visa trong 7 ngày gần nhất`.
- Anh Bảo chốt lại thứ tự cuối report Australia: `5. Tin tức báo chí` → `6. Ghi chú quan trọng` → `7. Nguồn thông tin` (tách `Web chính phủ` và `Link báo chí`) → `8. Technical changelog`.
- Anh Bảo chốt thêm rule vận hành cho Australia sheet: dùng workbook cố định `Australia Visa - Official Tracker`; mỗi lần chạy chỉ tạo/cập nhật tab ngày mới trong cùng file, không tạo spreadsheet file mới.
- Đã chuyển baseline `27Mar26` vào workbook `Australia Visa - Official Tracker` và cập nhật cron Australia để dùng workbook cố định này.
